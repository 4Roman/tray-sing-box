# TODO

Доработки, выбранные в работу (июнь 2026).

## В работу

1. ~~**Иконка трея, показывающая состояние VPN**~~ ✅ сделано: genicons рендерит второй, обесцвеченный `tray-off.ico`; `TrayUI.UpdateStatus` переключает иконку (цветная = работает, серая = остановлен) только при смене состояния.

2. ~~**Просмотр логов из web UI**~~ ✅ сделано: секция «Логи» в web UI (`GET /api/logs`): хвосты tray-sing-box.log и sing-box-лога (когда в config.json задан `log.output`), ручное и автообновление.

3. ~~**Git-репозиторий + CI**~~ ✅ сделано: проект под git, `.github/workflows/ci.yml` на windows-latest (`go test -short ./...`, genicons, сборка, `go-winres patch`, артефакт).

4. ~~**Подписки как сущность, а не разовый импорт**~~ ✅ сделано: `subscriptions.json` рядом с exe, секция «Подписки» в web UI (добавить/обновить/удалить), пункт трея «Обновить подписки», автообновление через минуту после старта и далее каждые 6 часов; URL в буфере обмена при импорте распознаётся как подписка; устаревшие ноды удаляются с перепривязкой маршрутов, VPN перезапускается только при реальном изменении конфига.

## Надёжность (по итогам аудита 21.09.2026)

1. ~~**Вывод sing-box в отдельный файл вместо pipe в лог трея**~~ ✅ `sing-box-console.log`, пишет сам sing-box; ограничен 10 МБ (ротация при старте + усечение на месте, пока работает).
2. ~~**Проверка «выжил ли» в `Manager.Start()`**~~ ✅ grace 1.5 с, в ошибке — текст самого sing-box (без ANSI); причина более поздней смерти — `LastExit()`, попадает в окно «автоперезапуск не помог».
3. ~~**Повтор `NIM_ADD`**~~ ✅ локальный форк `third_party/systray` (4 патча, `PATCHES.md`), ошибки библиотеки пишутся в лог. Возврат значка после перезапуска explorer.exe проверяет этап `explorer` теста в Windows Sandbox (`lifecycle.ps1`); модульных тестов у патчей нет.
4. ~~**Single-instance guard**~~ ✅ именованный мьютекс, fail-open.
5. ~~**Старт/стоп и автозапуск вне событийного цикла; один общий `opMu`**~~ ✅ воркер, явное желаемое состояние, popup’ы вне замка.
6. ~~**Работа с sing-box по полному пути/PID**~~ ✅ + реальные тесты жизненного цикла на подставном `sing-box.exe`; собственные `sing-box check/version` исключены через `process.HelperOutput`.
7. ~~**`CREATE_NO_WINDOW` без `DETACHED_PROCESS`**~~ — проверено 25.09 перезагрузкой в Windows Sandbox и **не делается**: sing-box со скрытой консолью при выключении Windows завершается так же принудительно (`exit status 0x40010004`) — события завершения сеанса консольным программам не приходят, их получают только службы. Штатное завершение по Ctrl+C sing-box ничего не пишет в лог и выходит с кодом 0, так что отличить можно только по коду выхода, который приложение и логирует. Мигания консоли у docker нет: своё `DOCKER_CONFIG` без помощников учётных данных.
8. ~~Индикация «восстанавливаю…»~~ ✅ статус «Запускается…» (трей + web UI), попытки можно отменить. ~~Кнопка включить/выключить в web UI~~ ✅. Осталось: `<Delay>` у логон-триггера (не критично).
9. ~~**Ручная проверка после развёртывания**~~ ✅ загрузка Windows с `intent=running`, самолечение задачи `SingBoxTray`, второй экземпляр — 24.09 на рабочей машине (VPN поднялся сам через 15 с после загрузки, с первой попытки); перезапуск explorer.exe, падение sing-box, выключение из меню, перезагрузка с выключенным и включённым VPN, выход, веб-интерфейс и первая настройка чистой установки — 25.09 автотестом в Windows Sandbox (`build/installer/sandbox/lifecycle.ps1`, 68 + 36 проверок на `v1.0.1`).

## Установщик и автообновление приложения

0. ~~Подготовка и первый релиз~~ ✅ (25.09): `config.Version` + `--version`, иконки через `go:embed`, `release.yml` + `tools/relkey`/`relsign`; репозиторий `4Roman/tray-sing-box`, ключ создан (закрытый — офлайн), `AppUpdateRepo`/`AppUpdatePublicKey` заданы, релиз `v1.0.0` собран CI, подписан офлайн (`relsign -key-file`) и опубликован.
1. ~~Single-instance мьютекс~~ ✅ — обязателен до автообновления: после самоперезапуска планировщик перестаёт отслеживать процесс, `IgnoreNew` больше не защищает (проверено).
2. ~~Самообновление~~ ✅ `AppUpdateService` + `infrastructure/selfupdate`, пункт трея, секция web UI, суточная проверка. Полный цикл проверен 25.09 на настоящих релизах в Windows Sandbox (этап `update` теста `lifecycle.ps1`): установленная `v1.0.0` через 2 минуты сама предложила `v1.0.1`, скачала и проверила подпись и сумму, перезапустилась в новую версию и подхватила работающий sing-box (тот же PID), повторно не предлагала.
3. ~~`paths` + портативный режим~~ ✅ `internal/infrastructure/paths`: переносная (всё рядом с exe) и установленная (exe в Program Files, данные в `%ProgramData%\SingBoxTray`) раскладки, определяются по расположению exe без настроек; sing-box работает с рабочей папкой = папка данных.
4. ~~Inno Setup + CI-job; флаги; миграция старой папки~~ ✅ `build/installer/tray-sing-box.iss` (шаг «Build installer» в `release.yml`, установщик попадает в черновик релиза), ключи `--quit` / `--enable-autostart` / `--disable-autostart` / `--uninstall-cleanup`, перенос прежней переносной копии по `<Command>` задачи (`internal/infrastructure/migrate`). Проверено в Windows Sandbox (43/43, и на локальной сборке, и на файлах релиза `v1.0.0`): установка поверх работающей переносной копии, обновление без обрыва VPN, удаление, переустановка, `/DELETEDATA=1`. Автотест в Windows Sandbox — `build/installer/sandbox/run.ps1`.
5. ~~**Установка на чистую машину (без переносной копии)**~~ ✅ (25.09): пока `config.json` нет, страница настроек показывает «Первоначальная настройка» — кнопку «Скачать sing-box» и поле для первого конфига (`POST /api/config/create` → `Editor.CreateConfig`: один раз, никогда поверх существующего — жёсткая ссылка или эксклюзивное создание, обязательно после `sing-box check`). Целый конфиг достаёт до разделов, которых раньше через приложение не было, поэтому guard для них стал разрешающим списком (разделы, типы inbound и endpoint) плюс `*_path`/`*_directory`, `masquerade`, `ntp.write_to_system`, прослушивание не на loopback, `experimental.debug` (ревью: 16 подтверждённых находок, все учтены). Подсказки в ошибках запуска, импорта, подписок и обхода DPI ведут на эту страницу; подписка без конфига не сохраняется пустой, контейнер DPI без конфига не запускается.

6. ~~**GitHub Actions на Node 24**~~ ✅ (25.09): `checkout` v7.0.1, `setup-go` v7.0.0, `upload-artifact` v7.0.1, `action-gh-release` v3.0.3 — закреплены по SHA коммитов; у CI токен только на чтение. Сборка exe воспроизводима бит в бит (пересборка `v1.0.0` из тега совпала с релизом), `build/release/finish-release.ps1` подписывает только совпавший exe и собирает установщик локально.

## Сильно потом

- ~~**Проверка, что VPN реально работает, а не просто запущен**~~ ✅ сделано: проба generate_204 каждые 30 с через локальный inbound-прокси из конфига (или напрямую при TUN), дебаунс на 2 неудачи, статус «Запущен (нет связи)» в трее и в web UI.
- **Подпись кода (Authenticode)** exe и установщика: без неё SmartScreen и антивирусы относятся к новому exe с подозрением (антивирусная эвристика блокировала даже тестовый бинарник пакета `process`). Нужен сертификат — деньги и/или EV-токен.
- ~~**Конфиденциальность веб-интерфейса**~~ ✅ (23.09): одноразовая ссылка входа, секрет сеанса только в памяти страницы (не cookie — они не привязаны к порту, не `sessionStorage` — браузер пишет его на диск), выход при закрытии страницы; пароли/uuid/ключи маскируются «(скрыто)» и подставляются обратно, только если сервер не изменён; адреса подписок — без токена. Граница: учётные данные протоколов без проверенного TLS (vless/trojan без TLS, socks, http, ssh без `host_key`, `tls.insecure`) не защищены — их и так видит любой на пути по сети.
- ~~**Подмена «VPN запущен»**~~ ✅ (23.09): своим считается только `sing-box.exe` с повышенными правами (уровень целостности High); запущенный программой без прав не принимается и завершается перед стартом.
- ~~**Приватное пространство имён для мьютекса и события выхода**~~ ✅ (23.09): граница «Администраторы + High» (создать может только повышенный процесс), DACL пространства — только SYSTEM и администраторы; установщик узнаёт «был ли запущен» по коду выхода `--quit`.
- **Остаток после закрытия трёх рисков** (не чинится в приложении, записано как граница): сеанс веб-интерфейса — это управление конфигом, поэтому программа, перехватившая одноразовую ссылку раньше браузера (или помешавшая браузеру её открыть), получает сеанс до его окончания; протоколы без проверенного TLS можно перенаправить (DNS, промежуточный узел) и прочитать их учётные данные. Автотеста «процесс без повышения не открывает пространство, созданное повышенным» нет (нужен повышенный создатель).
- **Интеграция с Clash API sing-box** (`experimental.clash_api`, external_controller на 127.0.0.1) — трафик/скорость, активные соединения, замер задержки outbound, переключение selector без рестарта процесса. Фундамент для нескольких пунктов ниже.
- **Тест задержки серверов и «выбрать самый быстрый»** в web UI (поверх Clash API).
- ~~**Автоперезапуск при падении sing-box + уведомление**~~ ✅ сделано: монитор перезапускает процесс, пока он не работает при интенте «запущен» (в т.ч. после неудачного восстановления при загрузке), до 5 попыток с растущей паузой, сброс счётчика после 60 с стабильной работы или явного включения/выключения, MessageBox с последней ошибкой при исчерпании попыток.
- **Zapret без Docker** — нативный `winws.exe` (WinDivert) как альтернативная реализация `DPIBypassManager`; убирает зависимость от Docker Desktop. Бонус: автоподбор стратегии (аналог blockcheck) вместо ручного `dpi-params.txt`.
- **Структурный редактор конфига в web UI** — табличный вид route rules (домен/гео → outbound) поверх существующей валидации `sing-box check`; автообновление rule-set/geosite.
- ~~**История версий конфига**~~ ✅ сделано: каждое сохранение архивирует прежний конфиг в `config-history/` (последние 20), секция «История конфига» в web UI со списком и откатом; откат проходит обычный путь сохранения (валидация + архив), т.е. обратим.
